# Loom Alpha 6 recovery protocol policy.
# No state transition is automatic until a real first-stage handoff reports the generation used.
LOOM_EARLY_STATE_ROOT=/metadata/loom/state
LOOM_EARLY_SNAPSHOT_ROOT=/metadata/loom/early
LOOM_EARLY_AUTO_ARM=0
LOOM_EARLY_AUTO_CONFIRM=0
LOOM_TAKEOVER=0

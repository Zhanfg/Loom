# Loom Alpha 5 prepare-only early snapshot configuration.
# This does not enable first-stage takeover. It only prepares a sealed snapshot under /metadata.
LOOM_EARLY_PREPARE_ENABLED=0
LOOM_METADATA_MOUNT=/metadata
LOOM_TAKEOVER=0

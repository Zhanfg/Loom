# Loom Alpha 1 sidecar configuration
# This mode never replaces /system, /vendor, /product, or an existing module mount.
LOOM_SIDECAR_ENABLED=1
LOOM_TARGET=system
LOOM_ORIGIN=auto
LOOM_DM_NAME=loom-sidecar-system
LOOM_MOUNTPOINT=/data/adb/loom/mnt/system
LOOM_FILESYSTEM=auto

name: stage34-legacy-full-big-partial-diagnostic

on:
  push:
    branches:
      - feat/stage34-erofs-legacy-full-big-partial-tail
    paths:
      - .github/workflows/linux-hard-gate.yml

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Install erofs tools
        run: |
          sudo apt-get update
          sudo apt-get install -y erofs-utils
      - name: Probe legacy full-big partial EOF
        run: bash tests/integration/stage34_probe_legacy_full_big_partial.sh
